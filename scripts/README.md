# Scripts

`bootstrap-next-app.sh` is a reference script for creating the base app. Use it only in an empty directory before copying the starter pack.

For an existing app, follow `SETUP_COMMANDS.md` manually.
